#include "main.h"

/**
 * reset_to_98 - takes a pointer and updates the value it points to to 98
 * @*n : test value the pointer  points to
 */
void reset_to_98(int *n)
{
	_putchar("Address of variable 'n': %p\n", &n);
	return (0);
}
