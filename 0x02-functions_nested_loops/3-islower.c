#include "main.h"
/**
 * description:  function that checks for lowercase character
 * returns 1 if c is lowercase
 */
int _islower(int c)
{
	int x;
	if (x >= 'a' && x <= 'z')
	{
	 	return (1);
	}	
		else
		 return (0);
	
}


