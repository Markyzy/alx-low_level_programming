#include <stdlib.h>
#include "main.h"

/**
 * main - check if character is uppercase.
 *
 * Return: 1 if character is uppercase and 0 if not.
 */

int _isupper(int c)
{
	if (c = 'A' && c <= 'Z')
	{
		return (1);
	}
	else
		return (0);
}
