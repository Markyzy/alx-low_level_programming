#include "main.h"

/**
 * print_triangle - prints  triangle, followed by a new line;
 * @size: size of the traiangle
 */
void print_triangle(int size)
{
	if (size < = 0)
	{
		int i;
		for (i = 1; i <= size; i++)
		{
			int j;
			for (j = 1; j <= i; j++)
			{
			_putchar('#');
			}
			if (size <= 0)
		
				_putchar('\n')
		}
				return (0);
}
}
